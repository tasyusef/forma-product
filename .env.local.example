# Anthropic API — used for interpretation, prompt composition, and layer prompt merging
ANTHROPIC_API_KEY=sk-ant-...

# Fal.ai — used for image generation and inpainting
FAL_KEY=...

# Optional: spend guardrails (daily cap in USD, enforced in /api routes)
FAL_DAILY_USD_CAP=5
